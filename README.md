# 2-2-Exercises
Text01 -> I deleted the whitespace to show that whitespace isn't important in Java\

Text02 -> I removed the curly brackets and added a semicolon to show how important it is to have them in the right spots

Text03 -> I added a drawLegs(); to make the program draw the cat's legs. Every program starts running from the main method
